#include<stdio.h>
#include<cs50.h>
#include<string.h>

int main(int argc, string argv[])
{
    printf("%lu\n", strlen(argv[1]));
}

void atio(string word)
{
    // Finding the length of string
    int n = strlen(word);

    
}